Датчик (псевдо)случайных чисел

x[n+1] = f(x[n])
  f - какая-то хитрая формула

  f(x) =  (a*x + b) mod m
    a, b, m - хорошо подобранные числа
  Линейный конгруэнтный датчик

x[0] ?

  Константа: последовательность будет выглядеть хаотично, но будет одной и той же во всех пусках

  int rand();
    возвращает следующее число

  void srand(int seed);
    seed random - засей случайность
    текущее число выставляется в переданное значение
stdlib.h

  int time(int);
    количество секунд, прошедших с указанного момента

    нулевой момент - это 00:00:00 01.01.1970.

   time(0) === time(NULL) - это текущий момент времени (в летоисчислении языка Си)


   srand(time(NULL));
   // начальная разгонка ДСЧ


